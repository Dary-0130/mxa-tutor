# TASK-307: Evidence Citation Enforcer(证据引用强制器)

## 状态

🟢 v0.3(R2 微补丁:加硬 2 trap 自反修复 + 验收 #15 grep 守门补 `|| true`;R2 主拍板 D1-D5 通过 + 加硬 1 通过 + 微调通过;GPT 明示修完直接进 Codex;架构师自查 KPI 累积升级;架构师第二十三任)

---

## R1 反馈台账(2026-06-07,GPT Pro 一审 8 条全采纳)

> **R1 主判定**:不直接进 Codex,需 v0.2 最小修订;**主设计 D1-D3 不需要推翻**,1 P0 + 5 P1 + 2 P2 局部修订后**条件通过,不必升 R2**(GPT 明示)。

| # | 严重度 | R1 反馈 | v0.2 修订位置 | 反例溯源 |
|:-:|:-:|---|---|---|
| **P0-1** | P0 | 验收 #13 让 Codex 写 03 索引到 `✅` + 推进度条 5/7→6/7,违决策 07 + TASK-304 v0.5 已踩同款 P0 | § 验收 #13 改 `🔲 → 🔍`,不写 ✅,不动 5/7;§ 关联文档加决策 07;§ 给 Codex 提示加新条 #9 | **反例 28 + 反例 30 同源**(架构师无 repo 写治理流程,治理规则 vs 验收口径跨段同步漏)|
| **P1-1** | P1 | "三 case" vs "四 case" 跨段不一致(D3 表 4 行 + 接口契约 4 case + 文字 3 case)| § D3 / § 接口契约 / § 范围 / § 给 Codex 提示 / Stage 1 Step 3 / 验收 #2 全统一"四 case";措辞分类精化(架构师 v0.2 加硬,与 GPT 原措辞略异)| **反例 29 严重命中**(写完 v0.1 终稿前漏 grep "三 case" 跨段同步)|
| **P1-2** | P1 | `project_overview`(chunk 层)vs `overview`(chat 层)两层命名易混;Codex 在 `make_hit(...)` 误传 chunk 层值会让 D3 守门失效 | § D3 加映射说明段 + § Stage 0 #7 加注意事项 + § 接口契约伪代码显式标 chat 层 + make_hit 示例加注释 + § 给 Codex 提示加新条 #8(**架构师 v0.2 加硬,不止文字说明,接口契约伪代码示范正确写法 + 反例错误写法**)| **反例 28 严重命中**(架构师无 repo 凭印象写跨层命名,未实地核查 304 映射表)|
| **P1-3** | P1 | Stage 0 #5 / 验收 #6 grep 命令 0 命中 exit 1 + `tools/` 不存在会误阻断;Stage 0 查三处验收只查一处口径不一致 | § Stage 0 #5 改 `2>/dev/null \|\| true` 简洁 shell + 处理目录不存在 + 验收 #6 范围对齐 Stage 0 三处(**架构师 v0.2 微调:不用 GPT 提议的 Python 扫描脚本,简洁 shell 等价 + 不引入新维护负担;请 R2 拍板偏好**)| **反例 25 同源**(grep 工具默认行为 — 第二十一任 KPI 同源)|
| **P1-4** | P1 | 集成测试用真实 LLM 不稳定(LLM 偶发引用 / 不引用),需 fake TextProvider 写死触发 `invalid_or_missing_citations` 路径 | § 接口契约加集成测试 fake LLM 模板 + § 验收 #3 改 fake LLM 写死触发路径 + § 给 Codex 提示加"集成测试必用 fake TextProvider" | **测试设计层面**(非反例累积;Step 7 直降的稳定触发只能 fake)|
| **P1-5** | P1 | 验收 #9 硬写 `mypy features/chat/` 与 Stage 0 #6 "以 CI 实测为准" 自相矛盾 | § 验收 #9 改 "CI 全绿;具体命令以 Stage 0 #6 实测 `.github/workflows/ci.yml` 为准;PR 描述贴 Stage 0 #6 实测 CI gate 清单" + Stage 0 #6 纪律加强 | **反例 8 + 反例 26 同源**(make check vs CI 实际命令漂移 — 第十六任 KPI 同源)|
| **P2-1** | P2 | 验收 #7 hygiene 探针测试可能残留临时文件 `_tmp_hygiene_print_probe.py` | § 验收 #7 改 GPT 模板 + **架构师 v0.2 加硬**:加 `trap 'rm -f "$tmp"' EXIT` 兜底 race condition(GPT 原版用 if 块末尾 rm,中间失败会残留)| **工程细节**(非反例累积)|
| **P2-2** | P2 | Stage 1 Step 2 `pytest eval/` 若 eval/ 无 pytest 测试会 exit 5 假绿 | § Stage 1 Step 2 改 "若 eval/ 有 pytest 测试则跑 + 否则跑 CLI `--help` / dry-run / 小样例命令 + PR 描述贴输出" | **测试设计层面**(非反例累积)|

---

## R2 反馈台账(2026-06-07,GPT Pro 二审拍板 + 1 P0 + 1 P1)

> **R2 主判定**:主设计 D1-D5 通过 + 加硬 1 通过 + 微调通过;**加硬 2 hygiene trap 脚本自反(P0)+ 验收 #15 grep 同源漏(P1)**;v0.3 微补丁修完直接进 Codex,**不需 R3**(GPT 明示)。

| # | 严重度 | R2 反馈 | v0.3 修订位置 | 反例溯源 |
|:-:|:-:|---|---|---|
| **R2-加硬1** | 通过 | chunk 层 vs chat 层 显式标注 + 反例错误写法保留 | v0.3 不变 | (R2 主拍板)|
| **R2-加硬2** | **P0** | hygiene trap 脚本自反 — `trap '...' EXIT` 在 shell 退出时清理,但 `git status --short` 在 shell 退出前执行,**正常路径下 `_tmp_hygiene_print_probe.py` 必残留**,与 v0.2 "git status 不残留临时文件" 自相矛盾 + trap 措辞"无论 exit 路径都保证清理"凭印象写不覆盖 SIGKILL | § 验收 #7 抄 GPT v0.3 模板(`if` 块后显式 `rm -f "$tmp"` + grep 残留守门)+ trap 措辞改"覆盖正常退出和大多数中断 / 失败路径,不包括 SIGKILL" | **反例 28 新维度**("加硬反伤")+ **反例 27 同源**(凭印象写工具默认行为 — `trap EXIT` 触发时机)|
| **R2-微调** | 通过 | grep shell 简洁版接受;但不能单独作机器 gate,语义是"拿到清单 / 人工分类" | Stage 0 #5 + 验收 #6 文字微调 — 注明"非机器 gate,Codex 人工分类" | (R2 主拍板)|
| **R2-新P1** | P1 | 验收 #15 `grep -nE "\"project_overview\"" tests/features/chat/` 若当前 tests/ 无合法字面会 exit 1 — **R1 P1-3 同类风险,本任 v0.2 新增的 grep 又踩同款** | § 验收 #15 改 `2>/dev/null \|\| true` + 0 命中可接受 + 命中时只允许 chunk fixture 构造处 + PR 描述逐条列出 | **反例 30 N+1**(R1 P1-3 修了 grep 同源问题后,v0.2 新增 #15 又踩同款 — 反例 30 KPI 操作清单"同类变更同步所有应在段"漏)|
| **R2-跨段** | 闭合 | 四 case / fake TextProvider / 03 索引 🔍 / § 不做 跨段同步**无新主设计漏** | v0.3 不变 | (R2 主拍板)|

---

## 架构师自查 KPI 兑现声明(R1 抓住 5 处反例同源)

GPT R1 抓住的 8 条中,**5 处是反例同源**,本任新增累积:

| # | 反例 | 现象 | 漏抓根因 |
|:-:|:-:|---|---|
| 漏-1 | **反例 28** N+1(架构师无 repo 写治理流程)| P0-1 验收 #13 写 `🔲 → ✅` 直接踩 TASK-304 v0.5 已踩过的同款 P0,且 22 任交接表里 306 明示是 🔍 不是 ✅ | 我读过决策 07,但写验收 #13 时**未联动决策 07 + 22 任交接 + TASK-304 先例三处**;**这是反例 30 跨段同步漏的另一面**(跨决策 vs 验收口径)|
| 漏-2 | **反例 28** N+2 | P1-2 chunk 层 `project_overview` vs chat 层 `overview` 两层命名,我 Stage 0 #7 示例用了 chunk 层但 D3 helper 用 chat 层,**Codex 在 make_hit 测试代码里有概率误传 chunk 层值导致守门失效** | 我看过 TASK-304 R12 段但**未实地核查映射表**(304 § 745 那行已明示 chunk → chat 映射) |
| 漏-3 | **反例 29** N+1(跨段一致)| P1-1 D3 表 4 行 + 接口契约 4 case + 文字 3 case,**写完 v0.1 终稿前漏 grep "三 case" / "四 case" 跨段同步**(第二十任反例 30 KPI 同款) | 第二十任反例 30 KPI 明示"机械数数 ≠ 判断偏见;严禁以 'self-confirmation bias' 跳过机械数数",我没做这条 |
| 漏-4 | **反例 30** N+1(跨段同步漏的治理规则维度)| P0-1 治理规则(决策 07)vs 验收 #13 写法跨段同步漏 — 反例 30 是写完 v0.x 终稿前 grep "应该在所有段同步" 没做 | 与漏-1 同体两面;反例 30 KPI 操作清单"变更是否在 § 接口契约 / § 验收 / § Codex 提示 / § 风险 / § Stage 0 / § 关联文档全部应在段出现"我只看了 § 验收,**漏了 § 给 Codex 提示 + § 关联文档**(决策 07 没在 § 关联文档列出)|
| 漏-5 | **反例 25 + 26** N+1(工具默认行为)| P1-3 grep 0 命中 exit 1 + P1-5 mypy 路径硬写 — 都是凭印象写工具行为没实地核查 | 反例 25 第十五任 KPI 明示"任何写给 PM / Codex 的 grep 命令架构师本地实测一次再下笔",我没做;反例 26 第十六任 KPI 明示"验收清单 #4 必须用 `make check` 全管道",我硬写 `mypy features/chat/` 违反此 KPI |

### v0.2 → v0.3 R2 新增累积(2 处)

R2 抓出**两处新反例同源** + **一处反例 28 新维度**:

| # | 反例 | 现象 | 漏抓根因 |
|:-:|:-:|---|---|
| 漏-6 | **反例 28 新维度** N+1("加硬反伤")| 我 v0.2 加 `trap '...' EXIT` 想兜底 race condition,**正常路径下 `git status --short` 在 shell 退出前执行,trap 还没触发,临时文件必残留**,与"git status 不残留"自相矛盾。**100% 概率失败,比未加硬还糟** | 第二十二任反例 31 是"决策回避 / 软妥协 / 转嫁"(不敢拍硬决策);本任新维度是**反面同源 — "加硬时引入新 bug"**;两端都对架构师施压:不敢硬 + 硬错都翻车 |
| 漏-7 | **反例 27** N+2(工具默认行为)| 我写"无论 exit 路径都保证清理"凭印象;`trap EXIT` 不覆盖 `SIGKILL`(不可捕获信号)+ 不覆盖 shell 进程被外部杀死 + 不覆盖 `exec` 替换 | 反例 27 第十七任 KPI 明示"所有'工具默认行为'陈述前必须 cat 工具配置 / 文档实地核查";我应该 `man trap` 或测试 trap EXIT 触发时机 |
| 漏-8 | **反例 30** N+2(同源漏的"上一轮修订同类问题不全文 grep")| R1 P1-3 修了 Stage 0 #5 / 验收 #6 的 grep 0 命中问题后,我没全文 grep `grep` / `2>/dev/null` 跨段自查"还有哪些同源漏";v0.2 新增的验收 #15 又踩同款 grep 0 命中 | 反例 30 KPI 操作清单第 1 条"同类变更同步所有应在段",**我刚写完一轮还是没做到**;这是工艺纪律层面的硬伤 |

### 本任反例累积小结(v0.1 → v0.3 三轮)

| 反例 | 累积 N |
|---|:-:|
| 反例 28(架构师无 repo)| **N+3**(P0-1 决策 07 联动 + P1-2 chunk vs chat 层 + R2 加硬 2 反伤新维度)|
| 反例 29(跨段一致)| N+1(P1-1 三 vs 四 case)|
| 反例 30(跨段同步漏)| **N+2**(P0-1 治理规则 vs 验收 + R2 #15 grep 同源漏)|
| 反例 25 / 26 / 27(工具默认行为)| **N+2**(P1-3 grep / P1-5 mypy + R2 trap EXIT)|
| **本任合计** | **N+8** |

第二十二任交接时反例 28 N≥16 / 反例 29 N≥15 已超升仪触发器 N≥10;本任 +8 后**继续超**。**本任不升仪决策 09**(本任进 Codex 在即,不折腾治理流程;留 PM 在本任结束 + 306b 后评估)。

**第二十三任 KPI 候选**(等 v0.3 通过 / Codex 第二棒后,若反例形态稳定 → 升仪决策 09):

- 操作清单 — 写完每个 v0.x 终稿前必跑:
  1. grep 跨段:本任卡内所有数值表述("N case" / "N 条" / "N 行" / "N 步")grep 全文一致性
  2. **决策 07 联动核查**:任何"03 索引更新" / "Week 进度推进" / "状态字符变更"句子,grep 决策 07 + 最近 3 个先例任务卡(305 / 306 / 304)对齐
  3. **跨层命名映射核查**:本任涉及多模块时(features/chat 内部 vs 上游 chunk 层),实地核查上游任务卡映射表,**接口契约伪代码 + 单测示例必显式标层级**;不只文字说明
  4. **grep / shell 命令实测核查**:任何写给 Codex / PM 跑的 shell 命令,架构师本地或脑模拟实测一次 0 命中 / 命中 / 目录不存在三 case 后下笔
  5. **验收 vs CI 命令对齐核查**:任何"验收 #N 跑 X 命令" 描述,grep `.github/workflows/ci.yml` 实测对齐(决策 09 纪律 6 + 第十六任 KPI 接续)
  6. **(v0.3 新增,反例 28 新维度)加硬反伤防御**:任何"为兜底中断 / race condition / 失败路径加硬"动作,**架构师必须脑模拟正常路径**:加硬代码在正常路径会不会引入新失败?(如 trap EXIT 在 shell 内部检查前是否已触发);**反例 31 的反面同源** — 不敢硬 + 硬错都翻车
  7. **(v0.3 新增,反例 30 升级)同源漏自查**:任何 R 轮采纳后的修订(如 R1 P1-3 修了 grep 同源问题),**写 v0.x 终稿前必须全文 grep 同类形态**(本任 v0.2 新增的验收 #15 又踩同款 grep 0 命中 — 反例 30 KPI 操作清单"同类变更同步所有应在段"漏)

---

## v0.1 → v0.2 → v0.3 加硬 / 微调汇总(R2 全拍板)

**v0.2 架构师在 R1 反馈全采纳基础上做的 3 处加硬 / 微调,R2 全部拍板**;v0.3 加硬 2 微补丁修复自反 bug 后保留 trap 思路:

### 加硬 1 — P1-2 chunk 层 vs chat 层 不止文字说明(**R2 通过**)

**GPT R1 建议**:在 D3 或 Stage 0 #7 后补一段映射说明。

**架构师 v0.2 加硬**:不止文字说明,接口契约伪代码 + make_hit 示例显式标 chat 层值 + 加注释 + 给 Codex 提示加禁混用条 + 反例错误写法。

**R2 拍板**:**通过**。GPT R2 明示"这个加硬是有必要的,不建议退回'只写文字说明';做法比单纯文案更稳,能直接防 Codex 在测试 helper 里误传 chunk 层值"。**v0.3 保留**。

### 加硬 2 — P2-1 hygiene 探针 trap cleanup(**R2 方向通过,脚本必改 v0.3 微补丁**)

**GPT R1 建议**:验收 #7 改 if 块 + 末尾 `rm -f "$tmp"`。

**架构师 v0.2 加硬**:加 `trap 'rm -f "$tmp"' EXIT` 兜底 race condition。

**R2 拍板**:**方向通过,脚本必改 P0**。`trap '...' EXIT` 在 shell 退出时清理,但 `git status --short` 在 shell 退出前执行 — **正常路径下临时文件必残留**,与 v0.2 "git status 不残留" 自相矛盾;trap 措辞"无论 exit 路径都保证清理"凭印象,不覆盖 SIGKILL。

**v0.3 微补丁**:保留 trap(中途失败兜底),正常路径在 `git status` 前**显式 `rm -f "$tmp"`** + grep 残留守门;trap 措辞改"覆盖正常退出和大多数中断/失败路径,不包括 SIGKILL"。详 § 验收 #7。

**架构师反思**:这是**反例 28 新维度("加硬反伤")** — 加硬代码引入新失败路径,比未加硬还糟(100% 概率失败 vs 加硬前残留 race condition 低概率失败)。已写入 § 自查 KPI 兑现 漏-6,候选升仪决策 09 KPI 6。

### 微调 — P1-3 grep 不用 Python 脚本,用简洁 shell(**R2 通过**)

**GPT R1 建议**:用 Python 内联脚本扫描 `sys.stdout.write`。

**架构师 v0.2 微调**:用 `grep -rn "sys.stdout.write" eval/ scripts/ tools/ 2>/dev/null || true`,处理 0 命中 + 目录不存在。

**R2 拍板**:**通过 shell 版,不改 Python**。GPT R2 明示"shell 简洁版对这个场景够用";唯一注意:**不能单独作机器 gate**(`|| true` 后有命中也返回 success),v0.2 语义是"拿到撤回清单 / 人工分类",可接受。**v0.3 文字微调**:Stage 0 #5 + 验收 #6 注明"非机器 gate,Codex 人工分类"。

---

## 上下文

**项目位置**:Week 3 第 7/7,Week 3 收口任务。

**核心事实**:约 80% 的 citation enforce 已在 TASK-205 / TASK-304 落地。Step 5(空召回 → `no_retrieval_hits` E 类降级)+ Step 7(`_parse_and_validate` 校验后 `citations==[]` → `invalid_or_missing_citations` E 类降级)+ `is_fallback` / `fallback_reason` 4 枚举已实现 citation 主路守门。

**本任真正增量**(窄而具体):

1. **R12 sentinel backend 消解**(核心):TASK-304 R12 / TASK-305 D7 / TASK-306 D11 三任明示推后续 backend 承接,本任接力
2. **`source_type` 强类型守门加固**:E 类 fallback helper 增 `RetrievalHit.source_type` 路径(取代单 file_path 字面)
3. **搭车 chore**:hygiene CLI 路径豁免 + `eval/` 撤回 `sys.stdout.write`

不做:不抽 `citation_enforcer.py` 独立 module / 不改 prompt yaml / 不引 retry / 不扩 ChatResponse|SourceRef schema / 不写 ≥90% 作为本任验收。(详 § 不做)

---

## 上游契约(实地核查锚点)

> 决策 09 纪律 1 守门:本段所有"既有产物"陈述以 Codex Stage 0 实测为准。

| 上游 | 已落地产物 | 本任消费方式 |
|---|---|---|
| TASK-205 | `ChatService.handle_chat` 7 步 + `_parse_and_validate` 校验五步 + `_build_and_persist_fallback` E 类降级 helper + `is_fallback` / `fallback_reason` 字段 | 不改 7 步主流程;仅改 E 类 fallback 拼 answer 的 helper(预期 `_short_hit_label`,Codex Stage 0 #1 实测) |
| TASK-304 | `RetrievalHit.source_type: Literal[8 类]`(含 `"overview"` chat 层值)+ `SourceRef.file_path` 哨兵透传 `__project_overview__` + chunk → chat 层 source_type 映射表(`project_overview` → `"overview"`,task-304 § 745)| **chat 层 `RetrievalHit.source_type == "overview"` 是本任唯一可用强类型路径**;chunk 层值 `"project_overview"` 在本任测试 / 守门里**禁止出现**(P1-2 联动) |
| TASK-305 | `qa_with_context.yaml` v0.2-rc system 段含 "证据类型说明" 8 类 + 教 LLM 不引用 `__project_overview__` 字面 + 教 LLM 用"项目总览"称呼 | 本任不改 yaml;LLM 偶发顽固引用时由本任 backend 兜底;替换文案"项目总览"与 305 prompt 教学口径对齐 |
| TASK-306 | 评测框架 5 脚本 + `citation_type_available_rate` 维度 + R12 sentinel 扣分规则(§ 7) | 本任完工后由 306b 跑评测验证产品级 ≥ 90% 目标(**不写入本任验收**) |
| 决策 07 | `docs/03_TASK_INDEX.md` 更新边界 — Codex 推 🔍 等待验收;✅ 由 PM merge 后改;进度条按 ✅ 计数(若沿用 304 v0.5 P0 修订口径)| 本任验收 #13 严守此边界(P0-1 兑现) |
| 02 § 161-180 | `features/chat/citation_enforcer.py` v2.1 设计占位 | **本任不实现**(D1 决定);保留架构图占位,实际逻辑沿用 chat_service 内 |
| 05 § 6 + § 7 | E 类输出 schema + 证据强制规则字面契约 | 本任 sentinel 替换文案("项目总览")必须符合 § 6 E 类语义 |

---

## 输入(前置依赖)

- **必须已完成**:TASK-204 / TASK-205 / TASK-303 / TASK-304 / TASK-305
- **必须读过文档**:01(宪法)/ 02 § 161-180 + § 597-603 / 04(工程规范)/ 05 § 5-7 / **决策 07(03 索引更新边界)**(v0.2 P0-1 加)/ 决策 09(架构师实地核查)/ 决策 11(async + logger 双不变量)
- **必须读过的关联任务文档**:`task-205-v0_2.md`(R2 P0-1 类型链路 + 7 步流程 + § 6.5 helper 关系)/ `task-304-vector-rag-v0_5.md`(R12 段 § 1086-1100 + § 745 chunk → chat 层映射表 + P0-3 v0.5 03 索引修订先例)/ `task-305-v0_1.md`(D7)/ `task-306-v0_1.md`(D11 + § 7 sentinel 扣分 + § 头 状态 🔍 先例)

---

## 输出(交付物)

### 修改文件(预期 — Codex Stage 0 反向核查后填实测路径)

| 路径 | 改动 | 估行 |
|---|---|---|
| `features/chat/_chat_persist.py`(或 chat_service.py,Codex Stage 0 #1 实测) | E 类 fallback 拼 answer 的 helper(预期 `_short_hit_label`)加 sentinel 替换 + source_type 双校验 | +15-25 |
| `scripts/check_repo_hygiene.sh` | `print()` 规则加 CLI 路径豁免段(`eval/` `scripts/` `tools/`) | +5-10 |
| `eval/run_eval.py` / `eval/make_scoring_queue.py` / `eval/finalize_scores.py` 等(Codex Stage 0 #5 实测命中清单) | `sys.stdout.write(...)` workaround 撤回 `print(...)` | 等量 |
| `docs/03_TASK_INDEX.md` | Week 3 TASK-307 行状态 `🔲 → 🔍`;**不写 ✅,不动 5/7**(P0-1 兑现) | +1 -1 |

### 新增测试

| 路径 | 测试范围 | 估行 |
|---|---|---|
| `tests/features/chat/test_chat_persist.py` 或 `test_chat_service.py`(Codex Stage 0 #2 实测) | 单元:sentinel 替换**四 case**(P1-1)+ 现有 E 类 fallback 行为不退化 | +50-70 |
| `tests/features/chat/test_chat_service.py`(集成段) | 集成:**fake TextProvider** 写死触发 `invalid_or_missing_citations` 路径(P1-4)+ E 类 fallback answer 不含 `__project_overview__` + 含"项目总览" | +30-40 |

### 不动文件(明示)

- `core/prompts/qa_with_context.yaml`(305 已落地)
- `features/chat/chat_schemas.py`(ChatResponse / SourceRef 字段不扩)
- `features/chat/_prompt_builder.py` / `_retriever.py` / `_keyword_scorer.py`(304 已落地)
- `core/domain/source_ref.py`(02 锁字段)
- `features/chat/chat_service.py::handle_chat` 7 步主流程(TASK-205 main freeze)
- `app/config.py`(本任不引入配置项)

---

## D1 — 范围聚焦(同 v0.1,不变)

(同 v0.1 D1 全文,R1 没动主设计。摘要):本任范围严守三件:(a) E 类 fallback helper 内 R12 sentinel 替换("项目总览");(b) helper 内 `RetrievalHit.source_type == "overview"` 强类型双校验;(c) 搭车 hygiene 豁免 + sys.stdout.write 撤回。理由 + 反对意见详 v0.1 D1。

---

## D2 — 守门位置(同 v0.1,不变)

(摘要):sentinel 替换 + source_type 双校验落在 E 类 fallback helper(预期 `_short_hit_label`,Stage 0 #1 实测)。不在 Step 6 / Step 7 / Step 4 / VectorRetriever。详 v0.1 D2。

---

## D3 — sentinel 替换规则(R1 P1-1 + P1-2 修订)

### chunk 层 vs chat 层 命名映射(v0.2 P1-2 新增)

> source_type 有两层命名,本任只用 **chat 层**;Codex 实施时**禁混用**:

| 层 | 字段 | 取值 |
|---|---|---|
| **chunk 层**(TASK-303 / TASK-304 上游)| `ChunkRecord.source_type` | `"project_overview"`(完整字面)|
| **chat 层**(features/chat 内部)| `RetrievalHit.source_type` | `"overview"`(VectorRetriever 映射后值)|

**TASK-304 § 745** 映射表明示 chunk `project_overview` → chat `"overview"`(VectorRetriever 内部完成转换)。

**本任纪律**:

- **helper 内部识别**:`hit.source_type == "overview"`(chat 层值)
- **单测 `make_hit(...)` 调用**:**显式传 chat 层值** `make_hit(source_type="overview", ...)`,**禁传** `"project_overview"`
- **集成测试从 ChunkRecord 构造 fixture 时**:必经过 VectorRetriever 映射后再断言 helper 行为,不直接断言 chunk 层值
- **接口契约伪代码 / make_hit 示例必显式标 chat 层**(详 § 接口契约)

### 双校验规则 + 四 case 守门(v0.2 P1-1 措辞统一)

```
# 伪代码(具体实现 Codex Stage 0 后定型)
def _short_hit_label(hit: RetrievalHit) -> str:
    # 双校验:chat 层 source_type 强类型 + file_path 字面
    if hit.source_type == "overview" and hit.source_ref.file_path == "__project_overview__":
        return "项目总览"
    # ... 现有逻辑保留
    return existing_logic(hit)
```

**替换文案**:`"项目总览"`(与 305 prompt yaml § 证据类型说明教学口径对齐)

**四 case 守门**(P1-1 — 1 个正常替换 + 2 个单字段污染防御 + 1 个常规回归):

| case | `source_type`(chat 层)| `file_path` | 期望行为 | 类型 |
|---|---|---|---|---|
| 1 | `"overview"` | `"__project_overview__"` | 替换为"项目总览" | **正常替换** |
| 2 | `"overview"` | 其他值(如 `"some_file.m"`)| 不替换,沿用现有 helper | **单字段污染防御**(source_type 对但 file_path 不符)|
| 3 | 非 `"overview"`(如 `"file"`)| `"__project_overview__"` | 不替换,沿用现有 helper | **单字段污染防御**(file_path 对但 source_type 不符)|
| 4 | 非 `"overview"`(如 `"block"`)| 其他值(如 `"model.slx"`)| 沿用现有 helper(常规逻辑)| **常规非 overview 回归** |

四 case 单测守门保证:任一单字段被污染都不会误替换,正常 case 替换准确。

---

## D4 — 验收口径(同 v0.1,不变)

(摘要):本任级用单元测试 + 集成测试守门;**不写**"`citation_type_available_rate ≥ 90%`"作为本任级验收(306b 才能验证产品级)。详 v0.1 D4。

---

## D5 — R 轮策略(v0.2 微调)

**A 档 R1 单轮 + PM Step B + Codex Stage 0 反向核查**三层兜底**已通过 R1**;GPT R1 明示"修完不需要 R2"。

**v0.2 触发 R2 的微条件**:

- v0.2 加硬 1 / 加硬 2 / 微调 1(§ v0.1 → v0.2 加硬汇总 3 处)需要 GPT R2 拍板偏好;若 GPT R2 全同意 → 直接进 Codex;若任一处 GPT R2 反对 → v0.3 微补丁,**不升 R3**

---

## 接口契约

### 改动 1:`_short_hit_label`(或同名 E 类 fallback 拼标签 helper)

**位置**:`features/chat/_chat_persist.py` 或 `features/chat/chat_service.py`(Codex Stage 0 #1 / #2 实测)

**签名**:

```python
def _short_hit_label(hit: RetrievalHit) -> str:
    """E 类 fallback 拼用户可见短标签;sentinel 替换 + source_type 双校验。"""
```

**实现增量**(v0.2 P1-2 加硬 — 显式标 chat 层):

```python
# 新增:sentinel 替换双校验
# 注意:hit.source_type 是 chat 层值 "overview",不是 chunk 层 "project_overview"
# 详 D3 § chunk 层 vs chat 层 命名映射
if (
    hit.source_type == "overview"  # ← chat 层值,VectorRetriever 已从 chunk 层 "project_overview" 映射
    and hit.source_ref.file_path == "__project_overview__"
):
    return "项目总览"  # 与 305 prompt yaml § 证据类型说明教学口径对齐

# 现有逻辑保留(Codex Stage 0 #1 实测后照搬)
# ...
```

### 单元测试守门(v0.2 P1-1 四 case + P1-2 加硬 chat 层显式)

**位置**:`tests/features/chat/test_chat_persist.py` 或 `test_chat_service.py`(Codex Stage 0 #2 实测)

```python
@pytest.mark.parametrize("source_type,file_path,expected_replacement", [
    # case 1 — 正常替换:chat 层 "overview" + 哨兵 file_path
    ("overview", "__project_overview__", "项目总览"),
    # case 2 — 单字段污染防御:source_type 对但 file_path 不符
    ("overview", "some_file.m", None),
    # case 3 — 单字段污染防御:file_path 对但 source_type 不符
    ("file", "__project_overview__", None),
    # case 4 — 常规非 overview 回归:正常逻辑
    ("block", "model.slx", None),
])
def test_short_hit_label_sentinel_replacement(source_type, file_path, expected_replacement):
    # 注意:source_type 必传 chat 层值(如 "overview"),不传 chunk 层 "project_overview"
    # 详任务卡 D3 § chunk 层 vs chat 层 命名映射
    hit = make_hit(source_type=source_type, file_path=file_path)
    label = _short_hit_label(hit)
    if expected_replacement:
        assert expected_replacement in label
        assert "__project_overview__" not in label
    else:
        # 不误替换:label 不含"项目总览"
        assert "项目总览" not in label


# ❌ 反例 — 禁止 Codex 这样写(chunk 层值会让 D3 守门失效)
# hit = make_hit(source_type="project_overview", file_path="__project_overview__")  # 错!
# 正确写法见上面 case 1
```

### 集成测试 fake LLM 触发路径(v0.2 P1-4 GPT 模板抄入)

**位置**:`tests/features/chat/test_chat_service.py` 集成段

**测试目标**:**非空召回 + LLM 未引用 → Step 7 E 类 fallback 拼短标签**(D3 真正要保护的路径)

```python
def test_overview_sentinel_replaced_in_e_class_fallback(real_project_with_chunks):
    # 1. retriever 返回至少 1 个 overview hit
    #    fixture 复用 304 P0-3 conftest 的 real_project_with_chunks(若含 overview chunk)
    #    或 Stage 0 #7 扩 fixture(若不含)
    
    # 2. fake TextProvider 返 valid JSON 但 citation_ids=[](触发 Step 7)
    fake_provider = FakeTextProvider(response={
        "answer": "（fake LLM 故意不引用证据）",
        "confidence": "medium",
        "citation_ids": [],
        "follow_up_suggestions": [],
    })
    
    # 3. ChatService 跑完整链路
    chat_service = make_chat_service_with_overview_hit(fake_provider)
    response = await chat_service.handle_chat(...)
    
    # 4. 断言 E 类 fallback 触发(Step 7 路径)
    assert response.is_fallback is True
    assert response.fallback_reason == "invalid_or_missing_citations"
    
    # 5. 断言 sentinel 替换生效
    assert "__project_overview__" not in response.answer  # 不泄露字面哨兵
    assert "项目总览" in response.answer                   # 含可读名
```

**关键纪律**:

- **禁用真实 LLM**(模型偶发引用 / 不引用 → flaky)
- **禁用 monkeypatch DeepSeek TextProvider**(违 04 工程规范测试隔离)
- **必用 fake TextProvider 写死返回值**(citation_ids=[])

### 改动 2:`scripts/check_repo_hygiene.sh`

**位置**:`scripts/check_repo_hygiene.sh`(Codex Stage 0 #4 实测现状 6 条规则)

**变更**:`print()` 检查规则加 CLI 路径排除(`eval/` `scripts/` `tools/`)。**具体实现 Codex Stage 0 #4 实测脚本现状后定型**。

**语义**:`print()` 在业务代码(`features/` `app/` `core/` `adapters/` `api/`)继续禁用;在 CLI 路径(`eval/` `scripts/` `tools/`)允许 — **修绿不绕规原则**:改规则匹配意图,而非改代码绕规则。

### 改动 3:`eval/` 内 `sys.stdout.write` 撤回 `print()`

**位置**:Codex Stage 0 #5 实测命中清单。

**变更规则**:每处 `sys.stdout.write(s + "\n")` → `print(s)`;每处 `sys.stdout.write(s)` → `print(s, end="")`。**Codex 必逐处看 context 决定 `end=` 参数,禁机械替换**(否则可能改变输出行为)。

---

## 范围(必须做)

- [ ] D1 范围锁定:R12 sentinel 消解 + source_type 守门 + 搭车 hygiene
- [ ] D2 守门位置:E 类 fallback helper 内
- [ ] D3 sentinel 替换规则:**chunk 层 vs chat 层映射 + 双校验 + "项目总览" + 四 case 守门**(v0.2 P1-1 + P1-2 修订)
- [ ] D4 验收口径:本任级单测 + 集成测试(fake LLM 写死触发路径,v0.2 P1-4 修订)
- [ ] D5 R 轮策略:A 档 R1 单轮 + PM Step B + Codex Stage 0 三层兜底
- [ ] **搭车 chore 1**:hygiene 脚本加 CLI 路径豁免
- [ ] **搭车 chore 2**:`eval/` 内 sys.stdout.write 撤回 print()
- [ ] Codex 实施分 **2 commit 单 PR**:commit 1 hygiene + 撤回;commit 2 sentinel 消解 + 单测 + 集成测试

---

## 不做(明确排除)

- ❌ **不抽 `citation_enforcer.py` 独立 module**(D1)
- ❌ **不改 `qa_with_context.yaml`**(D1)— 305 v0.2-rc 已搞;v0.3 等 306b 数据
- ❌ **不引入 retry / re-prompt 机制**(D1)— Step 7 直降 E 类已够
- ❌ **不扩 `ChatResponse` / `SourceRef` schema**(D1)— 影响 TASK-403 + 语义重叠
- ❌ **不动 `ChatService.handle_chat` 7 步主流程**(D2)— TASK-205 main freeze
- ❌ **不改 `VectorRetriever._to_retrieval_hit`**(D2)— 304 PM 拍 Z sentinel 原样透传
- ❌ **不写 "citation_type_available_rate ≥ 90%" 作为本任验收**(D4)— 306b 才能验证
- ❌ **不在搭车 hygiene 修改时改 hygiene 其他 5 条规则**
- ❌ **不在本任内推 03 索引到 ✅ / 不动 Week 3 进度计数**(v0.2 P0-1 — Codex 只推 🔍,✅ 由 PM merge 后改;决策 07 + TASK-304 v0.5 先例)
- ❌ **不混用 chunk 层 / chat 层 source_type 值**(v0.2 P1-2 — make_hit / 单测 / helper 内禁传 chunk 层 `"project_overview"`)

---

## 风险与注意点(R 段)

### R1 — Codex Stage 0 #1 实测 helper 路径与预期不符

(同 v0.1)

### R2 — source_type 双校验 vs 单 file_path 校验的取舍

(同 v0.1)

### R3 — 搭车 hygiene 撤回 sys.stdout.write 时语义变化

(同 v0.1)

### R4 — overview chunk fixture 复用 TASK-304 P0-3 conftest

(同 v0.1)

### R5 — hygiene 脚本改动可能触发 hygiene 自身规则

(同 v0.1)

### R6 — 决策 11 logger.exception + async 双不变量守护

(同 v0.1)

### R7 — chunk 层 vs chat 层 source_type 命名混用风险(v0.2 P1-2 新增)

**风险**:TASK-303 / TASK-304 chunk 层 `ChunkRecord.source_type == "project_overview"`(完整字面);features/chat 内部 `RetrievalHit.source_type == "overview"`(VectorRetriever 映射后);**Codex 写 make_hit 测试代码 / helper 守门时凭印象误传 chunk 层值,会让 D3 双校验失效**(case 1 永远不触发,sentinel 替换永远不生效)。

**应对**:

- D3 § 加 chunk 层 vs chat 层映射说明段
- Stage 0 #7 加注意事项
- 接口契约伪代码显式标 chat 层 + make_hit 示例加注释 + 反例错误写法(v0.2 加硬 1)
- 给 Codex 提示加新条 #8(禁混用)
- 单测四 case 全用 chat 层值

### R8 — 03 索引推进度条引起治理流程冲突(v0.2 P0-1 新增)

**风险**:Codex 实施完直接把 `docs/03_TASK_INDEX.md` 改 `🔲 → ✅` + Week 3 进度 5/7→6/7,**违决策 07 + TASK-304 v0.5 P0 先例**;PM 看到不一致需要打回 → 浪费一轮 PR。

**应对**:

- 验收 #13 严写 `🔲 → 🔍`,不写 ✅,不动 5/7
- 给 Codex 提示加新条 #9
- 关联文档加决策 07

---

## Stage 0(Codex 实地核查清单 — v0.2 #5 #6 #7 修订)

> **执行前**:Codex 必读 `decisions/20260603-09-architect-must-verify-not-assume.md` + **`decisions/20260601-07-task-index-update-not-docs-change.md`**(v0.2 P0-1 新加)+ 本任 § 上下文 + § 上游契约 + § D3 chunk 层 vs chat 层映射段。

1. **`view features/chat/_chat_persist.py`** 实地核查 helper 是否存在 + 完整签名 + 当前实现行号 + 与 `_build_and_persist_fallback` 的调用关系。**预期**:helper 在 `_chat_persist.py`(TASK-205 § 6.5 拆分后)或 `chat_service.py`(若未拆)。

2. **`view features/chat/chat_service.py`** 找 `_build_and_persist_fallback` 实现 + 该函数内调用拼 answer 的 helper 路径。

3. **`grep -rn "__project_overview__" features/ core/ adapters/`** 现有引用点(防御性 — 避免漏改)。**预期**:`features/chat/_retriever.py`(304 透传)+ helper 文件。若有额外引用 → 上报 PM。

4. **`view scripts/check_repo_hygiene.sh`** + **`view scripts/check_repo_hygiene.py`**(若存在)6 条规则全文 + `print()` 当前实现形态 + 现有排除路径。

5. **`grep -rn "sys.stdout.write" eval/ scripts/ tools/ 2>/dev/null || true`**(v0.2 P1-3 修订 — 处理 0 命中 + 目录不存在;**v0.3 R2 拍板补注**:**非机器 gate**,`|| true` 后有命中也返回 success;Codex 实测后**人工分类**命中清单:撤回 / 保留 / 上报 PM)— 拿到撤回清单。**预期**:eval/ 下 3-5 处;若 `tools/` 不存在或 0 命中,命令不报错(`|| true` 兜底)。

6. **`view .github/workflows/ci.yml`** 实地核查 CI gate 完整步骤清单(决策 09 纪律 6 + 第十六任 KPI;v0.2 P1-5 强化):**Codex 必在 Stage 0 报告中完整列出 ci.yml 里所有 `run:` 命令清单**;PR 描述也要贴此清单;验收 #9 以此清单为准,**禁拿 `make check` 或局部 mypy 当 CI 等价替代**(反例 8 + 反例 26 KPI 兑现)。

7. **`view tests/features/chat/conftest.py`**(304 P0-3 新建)— fixture 清单 + 是否含 overview chunk fixture + `real_project_with_chunks` 是否覆盖 overview chunk 路径。**注意(v0.2 P1-2 修订)**:fixture 中若构造 `ChunkRecord(source_type="project_overview", ...)`,**这是 chunk 层值**;经过 VectorRetriever 映射后 `RetrievalHit.source_type == "overview"` 才是本任 helper / 单测断言用的 **chat 层值**;Codex 必区分。

8. **`cat pyproject.toml`** `[tool.ruff.lint]` / `[tool.mypy]` / `[tool.pytest.ini_options]` 现状(反例 27 KPI 兑现)。

9. **`grep -nE "logger\.(exception|debug|info|warning|error|critical).*(query|question|source_text|chunk_text|chunk_id|answer|\{exc\}|repr\(exc\)|str\(exc\))" features/chat/`** 改动前 features/chat 已无违规(决策 11 守护)。

---

## Stage 1 实施步骤

### Step 1:hygiene 脚本路径豁免(搭车 commit 1)

1. 改 `scripts/check_repo_hygiene.sh`(或 `.py`):`print()` 规则加 CLI 路径豁免(`eval/` `scripts/` `tools/`)
2. 本地跑 `bash scripts/check_repo_hygiene.sh` 确认仍全绿
3. **commit 1**:`chore(hygiene): allow print() in CLI paths (eval/, scripts/, tools/)`

### Step 2:`eval/` 内 sys.stdout.write 撤回 print()(v0.2 P2-2 修订)

1. 按 Stage 0 #5 命中清单逐处看 context,决定 `print(..., end=...)` 参数
2. 本地跑 `bash scripts/check_repo_hygiene.sh` 确认全绿
3. **验证 CLI 行为不退化**(v0.2 P2-2):
   - 若 `eval/` 下已有 pytest 测试:跑 `pytest eval/`
   - 若无 pytest 测试:跑被修改的 CLI 脚本的 `--help` / dry-run / 小样例命令,**在 PR 描述贴输出**
   - 禁仅靠 `pytest eval/` 退出码作为绿(可能 exit 5 假绿)
4. 接续 commit 1

### Step 3:R12 sentinel 消解 + source_type 双校验

1. 按 Stage 0 #1 / #2 实测路径改 helper(预期 `_short_hit_label`)
2. 加单元测试**四 case**(v0.2 P1-1 — D3 表覆盖,**source_type 全用 chat 层值 `"overview"`,禁 chunk 层 `"project_overview"`**;v0.2 P1-2)
3. 加集成测试(v0.2 P1-4 — fake TextProvider 写死 `citation_ids=[]` 触发 `invalid_or_missing_citations` 路径)
4. 回归测试:非 overview case 文案与 main 一致
5. **commit 2**:`feat(chat): resolve R12 sentinel UX leak in fallback labels (TASK-307)`

### Step 4:验收 + CI

1. `make check` 全管道全绿
2. CI 全绿(以 Stage 0 #6 实测 `.github/workflows/ci.yml` 清单为准;PR 描述贴清单)
3. PR 描述贴本任 § 验收勾选清单 + Stage 0 实测报告 + 两 commit 解释 + CI gate 实测清单

---

## 验收标准(v0.2 P0-1 + P1-3 + P1-4 + P1-5 + P2-1 修订)

- [ ] **#1**:Stage 0 9 项全实测完成 + 报告 PM,所有"与预期不符"项已 PM 拍板(决策 09 纪律 1)
- [ ] **#2**:helper(实测同名)**四 case** 单测过(v0.2 P1-1 — D3 表 4 行 + 测试代码 source_type 全用 chat 层值)
- [ ] **#3**:集成测试 fake LLM 触发路径(v0.2 P1-4):
   - fake TextProvider 返 `citation_ids=[]` 触发 Step 7
   - `assert response.is_fallback is True`
   - `assert response.fallback_reason == "invalid_or_missing_citations"`
   - `assert "__project_overview__" not in response.answer`
   - `assert "项目总览" in response.answer`
- [ ] **#4**:回归测试 — 非 overview case 的 helper 输出与 main 一致
- [ ] **#5**:`bash scripts/check_repo_hygiene.sh` 在改动后全绿(eval/ 路径豁免生效)
- [ ] **#6**:`grep -rn "sys.stdout.write" eval/ scripts/ tools/ 2>/dev/null || true` 拿到清单后**Codex 人工分类**:eval/ 内全部撤回(本任范围);scripts/ tools/ 若有命中,上报 PM 决定本任搭车还是另起 chore(v0.2 P1-3 + **v0.3 R2 拍板**:非机器 gate,有命中也 success;`2>/dev/null` 处理目录不存在;PR 描述贴清单 + 人工分类结果)
- [ ] **#7**:hygiene 业务代码 print 探针 + 显式 cleanup + grep 残留守门(**v0.3 微补丁修复 v0.2 自反 bug**):
   ```bash
   tmp="features/_tmp_hygiene_print_probe.py"
   trap 'rm -f "$tmp"' EXIT  # ★ trap 兜底:覆盖正常退出和大多数中断/失败路径(不包括 SIGKILL)
   
   printf 'def probe():\n    print("should fail hygiene")\n' > "$tmp"
   
   if bash scripts/check_repo_hygiene.sh; then
     echo "FAIL: hygiene did not catch print() in features/"
     exit 1
   fi
   
   rm -f "$tmp"  # ★ 正常路径显式删除(必须在 git status 之前,否则 v0.2 自反 bug)
   
   status="$(git status --short)"
   printf '%s\n' "$status"
   if printf '%s\n' "$status" | grep -q "_tmp_hygiene_print_probe.py"; then
     echo "FAIL: temporary hygiene probe file was not cleaned up"
     exit 1
   fi
   ```
   验收口径:**正常路径** trap + 显式 `rm -f` 双重清理 → `git status --short` 不残留 `_tmp_hygiene_print_probe.py`;**中断 / 失败路径** trap 兜底(SIGKILL 除外)。**v0.2 自反 bug 教训**:trap EXIT 在 shell 退出时才触发,git status 在 shell 退出前执行,**必须 git status 前显式 rm**(详 R2 反馈台账 R2-加硬2)
- [ ] **#8**:`make check` 全管道全绿
- [ ] **#9**:CI 全绿(v0.2 P1-5 — 具体命令以 Stage 0 #6 实测 `.github/workflows/ci.yml` 为准;PR 描述贴 Stage 0 #6 实测 CI gate 完整 `run:` 清单;**禁拿 `make check` 或局部 mypy 当 CI 等价替代**)
- [ ] **#10**:`grep -nE "logger\.(exception|debug|info|warning|error|critical).*(query|question|source_text|chunk_text|chunk_id|answer|\{exc\}|repr\(exc\)|str\(exc\))" features/chat/` 0 命中(决策 11 守门)
- [ ] **#11**:PR 含两 commit(commit 1 hygiene + 撤回;commit 2 sentinel 消解 + 测试);commit 1 可独立 cherry-pick
- [ ] **#12**:无新增 module / schema / 配置项 / 依赖(D1 范围守门)
- [ ] **#13**(v0.2 P0-1 修订):`docs/03_TASK_INDEX.md` Week 3 TASK-307 行状态 `🔲 → 🔍`(等 PM 验收);**不写 ✅**;**不动 Week 3 进度计数 5/7**(✅ 由 PM merge 后改;**决策 07 + TASK-304 v0.5 P0 先例 + TASK-306 状态 🔍 平行先例**)
- [ ] **#14**:无 print 检查规则的其他 5 条 hygiene 规则被改动(D1 范围守门)
- [ ] **#15**(v0.2 P1-2 新增 + **v0.3 微补丁 grep 守门同源修复**):单元测试代码不混用 chunk 层值:
   ```bash
   grep -rn '"project_overview"' tests/features/chat/ 2>/dev/null || true
   ```
   验收口径:**输出允许为 0 命中**(`|| true` 处理 grep exit 1;`2>/dev/null` 处理路径不存在);**若有命中,只允许出现在 `ChunkRecord` / overview chunk fixture 构造处**;不得出现在 `make_hit(...)` / helper 单测参数 / helper 调用断言 / 普通注释示例中;**PR 描述必须逐条列出命中位置并说明合法性**。**v0.3 修复同源 bug 教训**:R1 P1-3 修了 Stage 0 #5 / 验收 #6 的 grep 0 命中问题后,v0.2 新增的 #15 又踩同款 — 反例 30 KPI 操作清单"同类变更同步所有应在段"漏(详 R2 反馈台账 R2-新P1)

---

## 估时

| 阶段 | 估时 |
|---|---|
| 架构师 v0.1 起稿 | 已完成 |
| GPT R1 一审 + 架构师采纳 → v0.2 | 已完成(本卡)|
| GPT R2 拍板 3 处加硬 / 微调 | 0.5h |
| PM 拍 v0.2 → 派 Codex | 0.25h |
| Codex Stage 0(9 项实测)| 1-1.5h |
| Codex Stage 1 Step 1-4 实施 + 测试 | 3-4h |
| PM Step B 核查 + CI 验绿 | 0.5h |
| **总计**(剩余) | **1.5 工作日** |

---

## 给 Codex 的提示(v0.2 P0-1 + P1-2 + P1-4 联动新增条)

1. **范围严守**:本卡 § 不做 段是硬约束,遇到诱惑扩范围必上报 PM。
2. **Stage 0 #1 / #2 实测优先**:helper 实际路径以实测为准;与预期不符 → 上报 PM。
3. **commit 1 / commit 2 拆分**:commit 1 hygiene + 撤回必须可独立 cherry-pick。
4. **决策 09 纪律 1 + 反例 28 兜底**:本卡每个"预期"都标 "Codex Stage 0 实测";实测发现不符 → P0 上报。
5. **决策 11 双不变量**:本任改动是纯函数 helper + 字符串替换,不涉及 logger / async;Stage 0 #9 + 验收 #10 加守门。
6. **R12 sentinel UX 文案对齐 305 prompt**:"项目总览"与 `qa_with_context.yaml` § 证据类型说明字面一致。
7. **测试 fixture 复用 304 P0-3 conftest**:Stage 0 #7 实测 + 缺 overview chunk fixture 时扩(不破 304 main freeze);禁建新 conftest。
8. **chunk 层 vs chat 层 source_type 禁混用**(v0.2 P1-2 新增):
   - helper 内部识别:`hit.source_type == "overview"`(chat 层值)
   - 单测 `make_hit(...)` 调用:**显式传 chat 层值** `make_hit(source_type="overview", ...)`,**禁传 chunk 层 `"project_overview"`**
   - 详 D3 § chunk 层 vs chat 层 命名映射;违反 → D3 守门失效(case 1 永远不触发)
9. **03 索引只推 🔍,不写 ✅**(v0.2 P0-1 新增):
   - 本任 PR 把 03 索引 TASK-307 行从 `🔲` 改为 `🔍`(等待验收)
   - **禁写 `✅`**(✅ 由 PM merge 后改;决策 07 + TASK-304 v0.5 P0 先例 + TASK-306 状态 🔍 平行先例)
   - **不动 Week 3 进度计数 5/7**
10. **集成测试必用 fake TextProvider**(v0.2 P1-4 新增):
    - **禁用真实 LLM**(模型偶发引用 / 不引用 → flaky)
    - **禁 monkeypatch DeepSeek**(违 04 测试隔离)
    - 必构造 fake TextProvider 写死 `citation_ids=[]` 触发 `invalid_or_missing_citations` 路径

---

## 关联文档(v0.2 P0-1 加决策 07)

- 关联任务:`docs/tasks/task-205-v0_2.md`(R2 P0-1 类型链路 + 7 步流程 + § 6.5 helper 关系)
- 关联任务:`docs/tasks/task-304-vector-rag-v0_5.md`(R12 段 § 1086-1100;P0-8 PM 拍 Z;**§ 745 chunk → chat 层 source_type 映射表;P0-3 v0.5 03 索引 🔍 修订先例**)
- 关联任务:`docs/tasks/task-305-v0_1.md`(D7 R12 sentinel 不承接 + 推 backend chore)
- 关联任务:`docs/tasks/task-306-v0_1.md`(D11 R12 sentinel chore + § 7 sentinel 扣分规则 + § 4.96 citation_type_available_rate 守门 + **§ 头 状态 🔍 平行先例**)
- **决策(v0.2 P0-1 新加):`decisions/20260601-07-task-index-update-not-docs-change.md`**(03 索引更新边界 — Codex 推 🔍,✅ 由 PM merge 后改)
- 决策:`decisions/20260603-09-architect-must-verify-not-assume.md`(纪律 1 / 反例 28 兜底)
- 决策:`decisions/20260604-11-async-blocking-and-logger-exception-bans.md`(双不变量守护)
- 宪法:`docs/01_PROJECT_CONSTITUTION.md` § 9 + 02 § 12 + 04 § 9(日志隐私三处硬约束)
- 风格:`docs/05_EXPLANATION_STYLE_GUIDE.md` § 5 D 类 + § 6 E 类
- 索引:`docs/03_TASK_INDEX.md` Week 3 TASK-307 行(状态 🔲 → 🔍 流程,v0.2 P0-1)

---

**版本**:v0.3
**起稿日期**:2026-06-07
**作者**:Claude(架构师,第二十三任)
**版本说明**:R2 GPT 二审拍板:主设计 D1-D5 通过 + 加硬 1 通过 + 微调通过 + 加硬 2 方向通过(脚本必修 P0)+ 新 P1 验收 #15 grep 守门同源漏(P1);v0.3 微补丁修复 v0.2 自反 bug(验收 #7 trap 自反 + 验收 #15 grep 同源)+ 架构师自查 KPI 累积升级(反例 28 新维度"加硬反伤" + 反例 27 N+2 + 反例 30 N+2);本任反例累积 N+8(反例 28 N+3 / 29 N+1 / 30 N+2 / 25-27 N+2)
**审批级别**:**GPT R2 明示修完直接进 Codex,不需 R3**;PM 拍板派 Codex 第一棒
